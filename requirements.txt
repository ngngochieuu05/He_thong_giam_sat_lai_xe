# ============================================================
# requirements.txt - Hệ Thống Giám Sát Lái Xe
# ============================================================

# --- UI Framework ---
flet>=0.21.0

# --- AI / Computer Vision ---
opencv-python-headless>=4.8.0   # headless cho server/Docker (không cần GUI)
ultralytics>=8.0.0               # YOLOv8
onnxruntime>=1.16.0             # ONNX Runtime cho inference

# --- Database ---
pyodbc>=4.0.39                  # Kết nối SQL Server qua ODBC

# --- HTTP / API ---
requests>=2.31.0                # Telegram API, Weather API, Gemini API

# --- Media ---
pygame>=2.5.0                   # Phát âm thanh cảnh báo

# --- Tiêu chuẩn (thường đã có sẵn nhưng liệt kê để rõ ràng) ---
Pillow>=10.0.0                  # Xử lý ảnh (dùng trong YOLO + Flet)
numpy>=1.24.0                   # Tính toán mảng (yêu cầu bởi OpenCV + YOLO)
